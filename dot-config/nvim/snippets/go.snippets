snippet	aocb "aoc base setup"
	import (
		"os"
		"fmt"
	)
	func run() int {
   	problem, err := getProblem(2023, ${1:1})
   	if err != nil {
   		fmt.Printf("%s", err)
   	}
		${0:$INSERT}
		return 0
	}

	func main() {
		fmt.Println("Here we go")
		os.Exit(run())
	}
